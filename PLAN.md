# 抓住大鹅（Catch The Goose）小游戏 — 开发方案

> **已定决策（2026-07-29 修订）**：游戏名《抓住大鹅》/ Catch The Goose；目标平台 **H5**；
> 引擎 **Cocos Creator 3.8 + TypeScript**。
>
> ⚠️ **本文档下半部分（第一~五节、第六节 1/5/6/7 小节）成文于目标平台还是微信小游戏的时期，
> 其中关于软著、AppID、开放数据域、主包 4MB、激励视频的内容均已作废。**
> 以「平台决策变更（2026-07-29）」一节为准，玩法与美术部分仍然有效。

> 参考素材：`assets-demo/`（竞品截图 + 录屏）
> 技术参考：`game-demo/ximalaya/`（赵云与阿斗，LayaAir 2.x H5 小游戏，主要参考其平台接入与外围系统）

---

## 一、玩法设计（核心循环）

品类：**3D 物理堆叠 + 三消**（"抓大鹅 / 捞玉"类，竞品截图即此玩法）。

### 核心规则
1. 木盒中随机倾倒一堆 3D 物件（每种物件数量为 3 的倍数），物理引擎让它们自然堆叠、翻滚。
2. 玩家点击盒中任意物件 → 飞入底部 **7 格暂存槽**。
3. 槽内凑满 **3 个相同物件**自动消除（合并动画 + 音效 + 完成度增加）。
4. **失败**：7 格被填满且无法消除；或倒计时归零。
5. **胜利**：盒中物件全部消除（完成度 100%）。

### 进度与激励
- 顶部：倒计时（如 5:00）+ 关卡完成度百分比。
- 结算：按最高完成度给星 —— **50% ⭐ / 70% ⭐⭐ / 100% ⭐⭐⭐**，星级对应奖励（道具礼包）。
- 记录**最好成绩**，接入排行榜（好友榜 / 全服榜）。
- **每日免费挑战 3 次**，次数用完可无门槛续次（H5 无激励视频，见平台决策变更一节）。

### 道具（3 个，与竞品对齐）
| 道具 | 图标 | 效果 |
|---|---|---|
| 移出 | 绿色回退箭头 | 把暂存槽中 3 个物件移出到临时栏，腾出空间 |
| 凑齐 | 磁铁 | 自动从盒中吸取 1 组（凑齐暂存槽中某物件的 3 连消） |
| 打乱 | 风扇 | 重新打乱盒中物品堆（解决遮挡/卡壳） |

获取途径：通关星级奖励、每日首次进入的礼包、用尽时无门槛补充（实现见 `grantProps`）。

### 难度曲线（关卡设计）
- 难度参数：**物件种类数 × 每种组数 × 时限 × 干扰项**。
- 当前曲线（每日 3 关，见 `LevelConfig.buildLevels`）：第 1 关 4 种 × 3 组 = 36 件 / 4:15 → 第 2 关 6 种 × 2 组 = 36 件 + 1 石头 / 3:20 → 第 3 关 9 种 × 2 组 = 54 件 + 2 石头 / 2:45；难度靠**种类数与时限**收紧，件数不再是主要梯度。
  第 1 关取 3 组是观感下限：件的大小按容器与件数反解，24 件填不满筐底，放大到填满就成了"几个大球"。
- 后期可加变体：冰冻物件（需先消除相邻才解锁）、限定顺序、隐藏在盒底的"金鹅"彩蛋等。
- 大白鹅定位：**吉祥物 + 稀有目标**。抓到鹅有专属动画/语音（"菜！"气泡），可作为每关的趣味目标。

---

## 二、技术选型

### 推荐：Cocos Creator 3.8.x（TypeScript）
选它的理由：
- **原生 3D + 内置物理**（Bullet/ammo.js），刚体堆叠、点击射线拾取开箱即用 —— 本玩法的核心依赖。
  ⚠️ 内置物理这条**在 2026-08-09 被推翻**：Bullet 的密堆永不收敛，已换成自接的 Jolt
  （见「物理引擎更换（2026-08-09）」一节）。选 Cocos 的其余理由仍然成立。
- **一键发布微信小游戏**，同时支持 H5 / 抖音 / 支付宝小游戏，微信生态工具链最成熟（分包、首屏加载、开放数据域排行榜都有官方方案）。
- TS 开发、组件化、可视化编辑器，UI/弹窗/结算这类外围系统开发效率高。

### 备选对比
| 方案 | 优点 | 缺点 |
|---|---|---|
| **Cocos Creator 3.8**（推荐） | 3D+物理+微信发布一条龙，生态最全 | 需学编辑器工作流 |
| LayaAir 3.x | 与 demo 同门，也支持 3D | 3D 生态和文档弱于 Cocos |
| Three.js + cannon-es + 小游戏适配器 | 轻量、纯代码可控 | UI/适配/分包全手写，外围系统成本高 |

> 注：game-demo 是 LayaAir **2.x 2D**，不能直接复用到 3D 玩法；它的价值在 `dialog/`（弹窗体系）、`data/rank.json`（排行榜数据结构）、分享/授权/体力这套运营逻辑，做外围系统时照抄思路即可。

### 关键技术点
1. **物理堆叠**：物件用凸包 / 方盒 / 圆柱代理（`JoltWorld.ProxyShape` 按件形状选），
   容器用模型网格本身（Jolt 的 `MeshShape` 吃三角汤，凹形无压力，取代了早期的分层环墙）。
   开局从盒子上方倾倒，靠引擎自己的休眠停下——**不再脚本冻结**，那是 Bullet 时代的补丁。
2. **点击拾取**：屏幕射线检测 → 命中物件 → 关闭其物理 → Tween 飞向槽位。
3. **槽位逻辑**：纯数据层（数组 + 排序归类），与竞品一致：相同物件自动相邻排列，凑 3 即消。
4. **性能**：物件总数控制在 30~60 个（当前第 3 关 56 件）；~~模型每个 ≤ 2k 三角面~~
   实际多在 2~10k 面，未成为瓶颈；贴图分辨率按实际显示尺寸定（容器 512² / 物件 256²）。
   ~~微信主包 4MB 红线~~ 已不适用，H5 看首屏传输量，模型在点「开始挑战」后才加载。
5. **录屏参考**：`assets-demo/*.mp4` 可逐帧对照动效节奏（飞入槽位、消除弹跳、道具表现）。

---

## 三、美术设计

### 风格基调（对齐竞品截图）
- **场景**：红木首饰盒（俯视 45° 固定相机）+ 背景木柜/玻璃展示格，暖木色调。
- **物件**：翡翠质感（高光滑、半透绿）+ 白鹅（纯白、橙色喙脚、戴珠串）。写实偏 Q，靠材质出质感。
- **UI**：卡通描边风 —— 黄底圆角按钮、蓝色标题弹窗、绿色草地进度条、金色星星（截图里已有完整范式）。

### 素材清单与生产方式
| 类别 | 内容 | 数量 | 生产方式 |
|---|---|---|---|
| 3D 物件 | 白菜、扳指、手串、弥勒、貔貅、平安扣、大鹅等 | 8~10 种 | AI 生成（Tripo3D / Meshy 文生 3D）→ Blender 减面修整 → glTF；或外包建模 |
| 3D 场景 | 木盒（或木盒 3D + 背景用高清贴图） | 1 套 | 盒子建模，背景直接用 2D 图省性能 |
| UI 图集 | 按钮、弹窗框、道具图标 ×3、星星、进度条、槽位框 | ~30 张 | AI 生图（即梦/Midjourney）出风格稿 → 设计切图 |
| 动效 | 消除粒子、飞入拖尾、星星弹出 | 若干 | 引擎粒子 + Tween |
| 音频 | BGM ×1 + 8 条音效 | 9 条 | **全部脚本合成**（`scripts/gen_bgm.py`），无版权尾巴 |

### assets-demo 够不够？
**够做风格参考，不够直接使用**：截图是竞品实拍，不能商用直抄（版权风险），且缺 3D 模型源文件、UI 切图、音频。需按上表重新生产同风格原创素材。录屏是动效还原的最好参考。

---

## 四、开发计划（单人 / 约 4~6 周）

### 里程碑 M1：核心玩法可玩（1~1.5 周）
- Cocos 3.8 工程搭建，微信小游戏发布链路跑通
- 木盒 + 物理倾倒（先用几何体占位）
- 点击拾取 → 飞槽 → 三消 → 胜负判定

### 里程碑 M2：完整单局体验（1~1.5 周）
- 正式 3D 物件模型替换占位
- 倒计时、完成度、星级结算、失败/胜利弹窗
- 三个道具实现 + 消除动效/音效

### 里程碑 M3：外围系统（1 周）
- 关卡/难度配置表（JSON 驱动，参考 demo 的 `data/weapon.json` 配置思路）
- 每日次数、最好成绩本地存储
- 分享得道具、邀请裂变（参考 demo 的 ShareLpDialog 逻辑）

### ~~里程碑 M4：平台接入与上线~~（已作废，2026-07-29）
转 H5 后整块取消：微信登录、开放数据域排行榜、激励视频广告位、提审上线均不再需要。
替代它的是 H5-1~H5-5 五项（见文末进度表），其中性能调优部分已完成
（引擎裁剪 + 资源瘦身：18MB → 8.2MB），只剩真机帧率验证未做。

---

## 五、目录规划（新工程）

> ⚠️ 下面是最初的规划，与实际落地结构已有出入（`ui/`、`props/`、`platform/`、`configs/`
> 都没有单独建目录，逻辑集中在 `core/`；关卡配置是 TS 而非 JSON）。实际结构如下：

```
catch-the-goose/
├── assets-3d/            # Blender 脚本生成的 glb 源模型（临时预览图不入库）
├── assets-demo/          # 竞品参考（保留）
├── game-demo/            # 赵云与阿斗源码参考（保留）
├── game/                 # Cocos Creator 工程
│   ├── assets/
│   │   ├── scenes/       # main
│   │   ├── scripts/core/ # 全部逻辑：Bootstrap / GameManager / HudUI /
│   │   │                 #   LevelConfig / SceneSkin / SlotTray / SaveData 等
│   │   ├── resources/    # ★ 全量打包目录：models / icons / textures / audio
│   │   └── models-pool/  # 未启用模型的候选池（不打包，见其 README）
│   ├── build-templates/  # 覆盖构建产物：web-mobile 的加载页与手机适配 CSS
│   ├── settings/         # 含 engine.json 的引擎模块裁剪清单
│   └── build/            # web-mobile 输出
├── design-qa.md          # 逐项验证记录（每次改动的地面真值都记在这）
└── PLAN.md               # 本方案
```

---

## 六、补充：此前未覆盖的关键项（2026-07-18 增补）

### 1. 上架资质与合规（周期最长，需最先启动）
- **微信小游戏 ≠ 随便发**：游戏类目上架需要 **软件著作权登记证书（软著）**，申请周期约 1~2 个月（加急更贵），这是整个项目的最长前置路径，应立即启动。
- **主体选择**：个人主体可发小游戏但类目/能力受限；企业主体才能完整开通流量主广告、虚拟支付。无内购的休闲游戏个人主体可行（流量主需累计 1000 UV 后开通）。
- **规避路径**：若资质周期不可接受，可先发 **H5 版**（自有域名/喜马拉雅等容器，参考 game-demo 的接入方式）或 **抖音小游戏**（资质要求类似，但审核节奏不同），玩法验证与上架流程并行。
- **诱导分享红线**：微信明令禁止"分享后才能获得奖励/复活"。竞品截图里"分享获得道具"在微信生态有违规下架风险 → 我们改为**激励视频广告**为主、分享仅作传播入口不强绑奖励。
- **命名与美术差异化**："抓大鹅"是品类通用词可参考，但整体 UI/物件不要与竞品一一对应复刻，建议起自有名字（如"翡翠盒子""鹅要发财"），美术保持同风格不同细节。

### 2. 字体 / 音频版权
- 中文字体商用授权是重灾区 → 只用免费可商用字体：**阿里巴巴普惠体 / 思源黑体 / 站酷系列**。
- 音频用 CC0 / 免费商用源（爱给网标注可商用的、freesound CC0），来源留档。

### 3. 死局与数值保护（玩法层查漏）
- 每种物件数量必须是 3 的倍数（生成器校验）。
- 槽位剩 1 格且盒中无可成对物件时的"必死"体验 → 打乱道具兜底 + 失败前一次救场
  （超时给 +60 秒、槽满/残局退回 3 件，每轮一次）。~~原设计是激励视频变现点~~，
  H5 下改为无门槛，按钮位置保留给将来的广告接入。真残局（谁都凑不出三个）
  会直接判负并说明原因，不会让玩家干等超时——加时救不了残局，只有腾格有意义。
- 关卡随机使用**固定种子**：同一关的物件顺序、投放点、旋转和初速度一致，便于回放调试。物理堆叠会受帧时序影响，最终静止形态允许存在差异，排行榜不依赖像素级相同遮挡。
- 反作弊：当前成绩纯本地（`SaveData`），~~微信开放数据域好友榜~~ 已不可用。
  排行榜暂缓，等真要做全服榜时再一并处理防刷分。

### 4. 后端策略
- **零后端**（现状）：每日次数、最好成绩、道具数量、皮肤、音效开关全部走
  `localStorage`（见 `SaveData.ts`，所有存档键集中在那一个文件）。
- ~~好友排行榜用微信开放数据域~~ 已不可用；~~微信云开发~~ 同理。
  后续真要做全服榜，H5 侧的轻量选择是 Cloudflare Workers + KV 或 Supabase 免费档。

### 5. 资源加载与包体红线
> ⚠️ 本小节的微信口径已作废，H5 的实际做法见文末「构建与自测（H5）」。

- ~~微信小游戏：主包 ≤ 4MB，整包（含分包）≤ 30MB。~~ H5 无硬性包体上限，
  但**首屏传输量**直接决定流失率，是比包体更真实的约束。当前构建 8.2MB，
  首屏代码 gzip 后约 520K，模型按需在点「开始挑战」后加载。
- 真机测试底线机型：iPhone 8 / 中端安卓（骁龙 6 系）保持 ≥ 30fps。**尚未在真机验证。**

### 6. 埋点与调优
- ~~微信小游戏自带数据分析~~ H5 没有现成的，要接需自己选（Umami / Plausible 等轻量自托管方案）。
  当前**未接任何埋点**。要调难度曲线时再补：每关开始/结束/失败原因，
  此品类核心指标是**第 3~5 关的流失率**（本作每天只有 3 关，对应第 2~3 关）。

### 7. 分工：需要用户完成的事项清单
> ⚠️ 转 H5 后，这张表里的微信相关事项（AppID、软著、开发者工具）**全部作废**——
> 那条 1~2 个月的最长前置路径已经不存在，随时可以上线。

现存的待办只剩：

| 事项 | 说明 | 紧急度 |
|---|---|---|
| 真机验证 | iPhone 8 / 骁龙 6 系安卓，看帧率与首屏耗时；目前只在桌面 Chrome 限速下验证过。**换 Jolt 之后更该测**：wasm 在中端安卓上的表现是当前唯一可能推翻现有方案的未知数 | ★★★ 最高 |
| 决定是否部署 | 当前决策是暂不部署；要上线时 Cloudflare Pages 免费免备案可直接用 | ★ 待定 |
| 新主题的模型 | ✅ 池塘农场与甜品小镇各补齐 9 个原创 Blender 模型、独立容器、槽位图标与场景底图；古玩铺另补玉玺/如意；地图 4 站全部开放且均为 9 种 | 已完成 2026-08-11 |

### 8. 开发进度（2026-08-11 更新）
| 里程碑 | 状态 | 提交 |
|---|---|---|
| M1 核心玩法（物理倾倒/拾取/三消/胜负） | ✅ | b9108f6 |
| M2 HUD/道具/结算/重开 + CDP 自动化自测 | ✅ | b670c42 |
| M2.5 竖屏适配 + 木纹材质 + 道具次数经济 | ✅ | b5ae515 |
| 打磨：柔和木纹/逃逸回收巡逻/按钮布局 | ✅ | f07b891 |
| 体验优化：11 种物件专属配色（Blender 烘进 GLB）、关卡推进+进度存储、8 个合成音效、结算弹窗（星星动画） | ✅ | （本次） |
| M3 完成：失败救场（槽满退 3 件/超时+60s，每轮一次）、每日 3 次+看广告补次数（MVP 占位）、各关最好成绩+新纪录、消除粒子/拾取爆点 | ✅ | （本次） |
| ~~M4 微信构建/登录/排行/广告~~ | ❌ 作废，见下 | |
| H5-1 补次数/补道具文案去广告化（无门槛续次，入口位保留） | ✅ | （本次） |
| H5-2 第二个每日主题「古玩铺」（jade 圆碗 + 9 件旧翡翠模型） | ✅ | （本次） |
| H5-3 首屏加载优化（引擎裁剪 + 加载进度条 + 手机浏览器适配） | ✅ | （本次） |
| H5-4 默认静音（HUD 声音键 / 暂停菜单均可开启） | ✅ | （本次） |
| H5-5 资源瘦身（删废弃字体/商标件 + 贴图降分辨率 + 未用模型出包），构建 10M → 8.2M | ✅ | （本次） |
| H5-6 BGM（40s 五声音阶循环，脚本合成，AAC 201KB） | ✅ | （本次） |

以下是 2026-07-29 ~ 08-11 这两周的工作，此前一直没进表：

| 里程碑 | 状态 | 提交 |
|---|---|---|
| 玩法扩展：金鹅彩蛋（藏堆底、不占格、即时给奖励） | ✅ | a372646 |
| 玩法扩展：冰封件（靠三消化开，每种最多冻 1 个以保证不死局） | ✅ | b947c72 |
| 场景主题手动可选并持久化（此前只按日期轮播，且换肤会切出「翡翠碗装水果」） | ✅ | e65fd48 |
| **物理引擎更换：Bullet → Jolt**（选型跑测 → 玩法层换血 → 容器 mesh 碰撞） | ✅ | f7f7f92 → 3b95cb3 → 3190f42 |
| 关掉 Cocos 内置物理模块，运行时省 460KB gzip | ✅ | dcb4eb7 |
| 挑战流程与游玩 HUD 重做（含游戏内「退出本局」） | ✅ | 91f11c5 |
| 挑战地图首页重构（三段式固定布局 + 可拖拽地图 + 路线延伸预告） | ✅ | 03753f9 / 80d28a6 |
| 第三主题「池塘农场」（9 件原创模型 + 藤编篮 + 场景底图 + 地图开放） | ✅ | （本次） |
| 第四主题「甜品小镇」（9 件原创模型 + 陶瓷托盘 + 场景底图 + 地图开放） | ✅ | （本次） |
| 古玩铺补齐玉玺/如意（大师关 7 种 → 9 种） | ✅ | （本次） |
| UI 分层：`scripts/core/ui/` 拆出 HomeScreen / UIRouter / UIKit / OnboardingScreen | ✅ | 03753f9 |
| 资源瘦身二轮：首页切图（`resources` 27M → 6.5M） | ✅ | 5448f9f |

### 平台决策变更（2026-07-29）

**放弃微信小游戏，专注 H5。** 软著（1~2 个月）+ AppID + 审核这条最长前置路径成本过高，
玩法本身不依赖微信能力。随之作废的整块工作：软著、AppID、微信登录、开放数据域好友排行榜、
主包 4MB 红线、微信广告 SDK、诱导分享合规约束。

配套的三项产品决策：

| 事项 | 决策 | 理由 |
|---|---|---|
| 每日 3 次限制 | 保留，但**无门槛续次** | H5 无账号体系、清缓存即重置，硬限制拦不住人却真实劝退新玩家；次数只当节奏提示。三处弹窗的位置保留，将来接广告只需换 onAction 回调。 |
| 排行榜 | **暂不做**，只保留本地最好成绩 | 没有开放数据域这个免费后端了，自建后端需处理防刷分；先验证玩法留存。 |
| 部署 | **暂不部署**，先本地跑 | 内容量和加载优化做完再考虑上线。届时 Cloudflare Pages 免费免备案可直接上。 |

H5 独有、原方案未覆盖的新问题：
1. ~~**首屏加载**~~ 已处理：引擎模块裁剪（55 → 12 个模块）+ release + `nativeCodeBundleMode=wasm`，
   构建 18MB → 10MB，js 总量 730K → **522K（gzip）**；加载页见 `build-templates/web-mobile/`。
2. ~~移动浏览器细节~~ 已处理：下拉刷新、双击缩放、地址栏 `100vh` 跳变，均在模板 CSS 里解决。
3. 模型版权复查：`resources/models/CREDITS.md` 里 `jingling`（精灵球）属宝可梦商标，
   已确保不进入任何主题的 family。

### 物理引擎更换（2026-08-09）

**Bullet → Jolt，自接 wasm，不再用 Cocos 内置物理。**

换的根因不是性能，是**密堆永不收敛**：件多起来之后 Bullet 的堆会一直微微蠕动，
不到静止。此前所有"稳定"的观感都是靠补丁撑的——落定后把刚体转 kinematic、
`PilePatrol` 定时巡逻纠位、`constrainVisualInside` 直接改写动态刚体的位置。
这些补丁本身又派生出新问题（换肤后角落的件卡在新围栏外、重开关卡漏清刚体留下幽灵堆），
且它们掩盖了真实的物理行为，越往后越难调。

决策方式是**建独立实验场同场跑测**（`lab/`，见其 README）：同一批件、同一个筐、
同一套参数与种子，四套底层各跑一遍，采落定时间 / 微颤 / 互插 / 覆盖率 / 抽底扰动。
四份结果留在 `lab/results/`。Jolt 胜出，落选的三套 POC 源码已删。

迁移带来的连锁变化：

- 容器碰撞从**分层环墙**改成**模型网格本身**（`MeshShape` 吃三角汤，凹形无压力）。
- 脚本冻结、`PilePatrol`、位置改写**全部删除**——件停下来的唯一途径是引擎自己的休眠。
- Cocos 内置物理模块从 `engine.json` 里摘掉，运行时省 **460KB gzip**。
- 代价是自己接 wasm：`scripts/gen-jolt-glue.mjs` 生成 `build-templates/web-mobile/jolt-glue.js`，
  `JoltLoader` 按固定路径原生 `import()` 它。**因此构建不能开 md5Cache**——
  产物改名即 404，物理静默不初始化，表现是"堆积效果变差"而不是报错。踩过一次，
  原因写在 `tools/build-web.sh` 的注释里。
- `lab/poc-b-jolt` 保留下来当**参数调优沙箱**：求解迭代与休眠阈值改之前先在那里复现，
  免得分不清是参数变了还是玩法逻辑变了。

#### ⚠️ 预览端口能打开，但物理完全失效

Cocos Creator 的“浏览器预览”（常见地址 `localhost:7457`）只启动编辑器预览服务器，
**不会把 `build-templates/web-mobile/jolt-glue.js` 复制到站点根目录**。因此入口页会返回 200，
但控制台随后报 `Failed to fetch dynamically imported module: /jolt-glue.js`；表面现象是页面像能开，
实际没有 Jolt，堆积、消除后的局部碰撞等物理反馈都会消失。该问题容易被误判为端口不可访问。

本项目涉及物理的验收一律执行 `tools/build-web.sh`，然后运行 `python3 tools/serve.py`，访问
`http://localhost:5185/`。排查顺序固定为：先看入口 HTTP 状态，再看浏览器控制台是否出现
`[JoltWorld] ✅ Jolt Physics 初始化完成`；不要只凭 Cocos 预览页是否显示来判断服务正常。

#### ⚠️ 偶发回归：物理正常，但堆积/消除看起来没有动画

这个问题与 `jolt-glue.js` 加载失败不是一回事，而且更隐蔽：**控制台不报错、物件能落定、
点击和三消都能完成，只是视觉反馈弱到像静态贴图。** 它通常在重构投放参数、替换平底容器、
或“清理看似多余的动画代码”后出现。

根因有两层：

1. 投放只靠根节点自由落体时，密堆里后生成的件会被已有物件遮住；旧版 0.15 秒单段缩放在
   手机上只有约 9 帧，又与下落同时发生，肉眼近似闪现。
2. Jolt 的 `ActivateBodiesInAABox` 只负责唤醒。平底托盘中的近邻若没有足够势能，即使已激活
   也几乎不位移；所以“摘掉刚体后唤醒接触岛”在逻辑上正确，视觉上却像没有碰撞。

当前不可删除的表现保障：

- `GameManager.spawnItems` 必须保留**视觉子树**的三段出现动画：`16% → 110% → 96% → 100%`，
  总时长 0.28 秒、逐件间隔 0.06 秒。只能缩放子树，不能 Tween 由 Jolt 控制位姿的根节点。
- `GameManager.pick` 必须在摘件后调用 `JoltWorld.kickAround`：普通摘件只影响最近 5 件，
  三消成立时影响最近 7 件并略加强。它施加真实线性/角冲量，不能替换成 Transform 假晃动，
  也不能改成 `wakeAll`（会让整堆像地震）。
- 新容器或物件尺寸改变后，冲量半径必须继续按 `itemScale` 推导，不写固定世界半径。

回归验收不能只看“能玩”：

- 必须测**构建产物**，390×844 下截开局后约 0.18 / 0.60 / 1.45 秒三帧，确认物件持续出现；
- 堆稳定后摘一件，检查 80 / 250 / 700ms 三帧：近邻应碰开并回落，远处件保持稳定；
- 连取同类三件，确认第三件触发后槽位清空、得分增长，并出现比普通摘件更明显的局部碰撞；
- 以上通过且控制台 0 warning / 0 error 才算完成。基线记录见 `design-qa.md` 顶部
  “堆积与消除碰撞动画回归”。

### 构建与自测（H5）

正式构建命令（务必带后两个参数，否则体积翻倍）：

```
CocosCreator --project <repo>/game --build \
  "platform=web-mobile;debug=false;sourceMaps=false;nativeCodeBundleMode=wasm;buildPath=project://build;outputName=verify"
```

- 引擎模块清单在 `game/settings/v2/packages/engine.json` 的 `includeModules`。
  **新增引擎能力（粒子、RichText、Mask、骨骼动画等）必须同步加进去**，否则运行时才报错。
- `game/build-templates/web-mobile/` 覆盖构建产物的 `index.html` / `style.css`（加载页与手机适配）。
- **自测端口别用 7456——那是 Creator 预览服务器占用的端口**，`http.server` 会静默失败，
  测到的其实是预览而非构建产物。用 7460 之类的空闲端口伺服 `game/build/verify`。
- 资源规范：**`resources/` 是全量打包目录**，放进去的东西不管有没有被引用都会进包体。
  新增模型/贴图前先确认真的会用；废弃的要删干净（含 `.meta`）。
- 贴图分辨率按**实际显示尺寸**定，不要一律拉满：容器占屏约 320px → 512²，
  物件约 90px → 256²。1024² 一张就要 5.59MB 显存，是手机上比文件体积更硬的约束。
- 物件碰撞体由代码生成（凸包 / Box / Cylinder），贴图不影响物理，但**减面会**——
  凸包点集直接从网格抽（`extractHullPoints`）。容器则是 mesh collider，换容器模型
  必须跑三关围栏回归。
- 未用于任何主题的模型放 `assets/models-pool/`（不打包），启用方法见该目录 README。
- ~~剩余体积大头仍是 `assets/resources` 5.3MB~~ 首页重构一度把它推到 27MB
  （设计参考稿和过程切图被放进了这个全量打包目录），2026-08-11 清回 **6.5MB**：
  参考图移去 `design/challenge-home/reference/`，过程切图删除，留用的 11 张按显示尺寸
  重采样、其中 6 张全不透明插画转 JPEG。做法与判据见提交 5448f9f。
- 2026-08-12 再次按运行时引用审计资源：删除 3 张已被连续长卷替代的地图分片和未启用的
  `bg_picnic`，站点圆章按 98px 实际显示尺寸从 512² 收敛到 320²；难度条、CTA、摘要框
  在保持尺寸的前提下转为 256 色 PNG，逐张 PSNR 为 37–38dB，圆章量化只有约 32.5dB
  因而没有继续压色。8 张 JPEG 统一以质量 90 渐进式重编码，逐张 PSNR 均不低于 36dB。
  `resources` 从约 11MB 降到 7.0MB，release/WASM 产物从约 19MB 降到 14.1MB；浏览器
  实测三档切换正常、控制台无 error。模型侧 40 个在用 GLB 合计
  约 1.97MB / 5.99 万三角面，无动画和骨骼，暂不为小体积收益冒碰撞轮廓变化风险。

自测基建：`scratchpad/regress2.py`（无头 Chrome + CDP，9 项断言）；注意 Creator 仅窗口获焦时重编译，脚本会自动 AppActivate 触发。

---

## 七、路线与已知缺口（2026-08-11）

### 现在站在哪

玩法闭环、外围系统、H5 适配、物理底层都已经完成，**工程侧没有明显的半成品**。
真正卡住项目的不是代码，是下面两件事。

### 已解除：内容量与难度口径

- 挑战地图上 4 站现已**全部可玩**；池塘农场与甜品小镇各有完整 9 件物件族。
- 古玩铺已补入原创玉玺/如意，family 从 7 件补到 9 件；四个主题的大师关现统一为
  54 个可消件 + 2 块石头，不再存在同档难度件数不一致。

### 缺口二：没有"明天再来"的理由

每天 3 关、进度跨天重置，玩家打完当天 3 关就没事可做。收集图鉴、成就、连续登录
这类留存钩子一个都没有；排行榜暂缓是合理决策（H5 无账号，自建后端要处理防刷分），
但它同时意味着现在完全没有跨日的目标。这块要做需要先想清楚**做给谁看**——
在没有埋点、没有真实玩家的前提下设计留存系统，是在赌方向。

### 缺口三：观测能力为零

没有任何埋点，难度曲线全靠主观判断。此品类的核心指标是中后段关卡的流失率，
本作每天只有 3 关，对应第 2~3 关。要调难度就得先能看见。

### 技术债（不紧急，但会持续拖慢）

- `GameManager.ts` 2030 行 / 106KB，`HudUI.ts` 1857 行 / 93KB。
  `scripts/core/ui/` 已经拆出 HomeScreen / UIRouter / UIKit，方向是对的，
  但游玩 HUD 还没跟上，仍是单文件承载全部 3D 玩法层与全部 HUD 绘制。
- 回归测试是本地 python + CDP 脚本，没进 CI，靠人记得跑。
- 构建依赖"Cocos 编辑器开着本工程"这个隐式前提（CLI 全量编译会产出运行时挂住的坏包），
  这条约束目前只活在 `tools/build-web.sh` 的注释里，自动化程度为零。

### 建议的下一步顺序

1. ~~合并 `feature/jolt-physics` 回 main~~ ✅ 2026-08-11 已合并（fast-forward，18 个提交）。
2. ~~清资源~~ ✅ 见上，`resources` 27M → 6.5M。
3. ~~重写进度表与路线图~~ ✅ 本节。
4. **真机验证**（iPhone 8 / 骁龙 6 系）。这是唯一可能推翻现有技术方案的未知数，
   排在所有内容工作之前。
5. ~~补模型开放剩余地图，并补齐古玩铺~~ ✅ 四站均已完成且每站 9 种；下一项建议先接
   埋点 + 部署，拿到真实数据后再决定是否继续扩地图。

---

## 八、发布可信化与验证闭环（2026-08-12）

按“先可信发布、再拿数据、最后决定内容”的顺序完成了工程内迭代：

- 构建脚本强制 release / WASM / 关闭 MD5 与 source map；构建前删除目标旧包，完成后检查
  Jolt、空壳脚本、生效配置与体积。当前正式目录约 **19MB 原始文件**，不等同于线上压缩
  传输量；此前 8.2MB 记录不是当前四主题首页版本的同口径数字，不再作为门禁。
- Jolt 初始化成为启动硬门闩；失败停在加载页并提供重新加载，不允许进入无物理对局。
- 恢复每日 3 次正式流程；用尽后仍可无门槛“再续一次”，但不再由测试开关静默穿透。
- 最好成绩升级为按 `themeId + level` 隔离，并兼容迁移 v1 旧存档。
- 模型缺失会阻止开局并提示重试，不再静默缩短关卡。
- CDN 两张关键切图增加本地 fallback，并合并同图并发请求；故障注入已验证页面完整。
- 新增 `tools/check.sh`：类型、四主题资源、发布开关、存档、观测事件、脚本语法与现有构建
  产物一并校验。
- 新增一方观测队列与运行时错误采集；默认不外发，部署时显式配置自有 endpoint。

仍需外部条件完成的事项：

1. 按 `docs/real-device-checklist.md` 在 iPhone 8 与骁龙 6 系真机验收。
2. 选择预览部署地址和事件收集端；当前未执行任何外部部署。
3. 邀请 20～50 名种子玩家跑满样本，使用 `tools/analyze-telemetry.mjs` 生成简报。
4. 达到样本门槛后按 `docs/product-decision-gates.md` 决定图鉴、每日目标或继续技术优化；
   数据不足前不扩新地图、不做排行榜。
